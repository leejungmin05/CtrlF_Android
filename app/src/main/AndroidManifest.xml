<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thinlineit.ctrlf">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CtrlF"
        android:usesCleartextTraffic="true">
        <activity android:name=".registration.RegisterViewActivity"/>
        <activity android:name=".registration.CompleteRegisterActivity" />
        <activity android:name=".registration.RegisterActivity" />
        <activity android:name=".registration.LoginActivity" />
        <activity android:name=".issue.detail.IssueDetailActivity" />
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page.PageActivity"/>
        <activity android:name=".MainActivity" />
    </application>

</manifest>